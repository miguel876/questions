import { styled } from '@mui/material'

export const StyledCircularProgressContainer = styled('div')(({ theme }) => ({
    position: 'absolute',
    top: 0,
    width: '100vw',
    height: '100vh',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
}))
